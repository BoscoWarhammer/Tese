\begin{algorithm}
    \SetKwInOut{Input}{Input}\SetKwInOut{Output}{Output}
    \Input{acting forces and torques in the bodies and the time step $\Delta t$}
    \Output{positions, velocities and accelerations to the next time step}
    \ForEach{body}{
        Calculate accelerations\;
        Correct velocites\;
    }
    \caption[Corrector]{Correction routine for state variables of bodies. Algorithm taken from \cite{Dissertacao}.}
    \label{alg:corretor}
\end{algorithm}
